[package]
name = "echobind-desktop"
version.workspace = true
edition.workspace = true

[[bin]]
name = "echobind-desktop"
path = "src/main.rs"

[dependencies]
eframe = { version = "0.35.0", default-features = false, features = ["default_fonts", "wgpu"] }
echobind-core = { path = "../echobind-core" }
openh264 = "0.9.7"
scap = "0.0.8"
serde_json.workspace = true
tracing.workspace = true
tracing-subscriber = "0.3.19"

[target.'cfg(target_os = "macos")'.dependencies]
apple-cf = { version = "0.9.0", default-features = false, features = ["iosurface"] }
screencapturekit = "8.0.1"
videotoolbox = "0.18.1"

[target.'cfg(target_os = "windows")'.dependencies]
# scap 0.0.8 uses the pre-1.5 Settings API. Keep Cargo from selecting the
# semver-incompatible windows-capture 1.5 release.
windows-capture = "=1.3.6"
